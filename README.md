# discord-update
Downloads and updates Discord

## Usage
Add executable permissions after downloading (just once)

`chmod +x discord-update.sh`

To run it call `./discord-update.sh` while inside the script's directory.

You can also copy the script to `/usr/local/bin` (`sudo cp discord-update.sh /usr/local/bin`) so you can call it from everywhere.
